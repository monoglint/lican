#pragma once

#include <filesystem>
#include <cstdint>

constexpr char PREF_DIR_SEP = std::filesystem::path::preferred_separator;

constexpr uint8_t LICAN_SHELL_VERSION = 1;
constexpr uint8_t LICAN_COMPILER_VERSION = 1;